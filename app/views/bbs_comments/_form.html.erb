<%= form_for(@bbs_comment) do |f| %>
  <% if @bbs_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bbs_comment.errors.count, "error") %> prohibited this bbs_comment from being saved:</h2>

      <ul>
      <% @bbs_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bbs_thread_id %><br />
    <%= f.number_field :bbs_thread_id %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_field :body %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
